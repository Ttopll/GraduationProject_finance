server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finance_system?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: tqc14279356zxc
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        jdbc:
          batch_size: 500
        order_inserts: true
        order_updates: true

app:
  security:
    jwt:
      secret: ${APP_JWT_SECRET:finance-system-dev-secret-key-please-change-32chars}
      expiration-minutes: 120
