server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finance_system?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: tqc14279356zxc
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
